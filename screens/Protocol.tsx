import React from 'react';
import { Text } from 'react-native';
import Card from '../components/layout/Card';

export default function Protocol() {
    return (
        <Card>
            <Text>Protocol screen</Text>
        </Card>
    );
}